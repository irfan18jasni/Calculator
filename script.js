function add(){
    // Get 2 use input numbers
    // put parseFloat at the top is better, so you can change it.
    const Num1 = parseFloat(prompt("Enter your first number"))
    const Num2 = parseFloat(prompt("Enter your second number"))

    // Perform calculations
    const result = Num1 + Num2

    // Perform result
    alert(`${Num1} + ${Num2} = ${result}`)
}

function minus(){
    // Get 2 use input numbers
    // put parseFloat at the top is better, so you can change it.
    const Num1 = parseFloat(prompt("Enter your first number"))
    const Num2 = parseFloat(prompt("Enter your second number"))

    // Perform calculations
    const result = Num1 + Num2

    // Perform result
    alert(`${Num1} + ${Num2} = ${result}`)
}

function multiply(){
    // Get 2 use input numbers
    // put parseFloat at the top is better, so you can change it.
    const Num1 = parseFloat(prompt("Enter your first number"))
    const Num2 = parseFloat(prompt("Enter your second number"))

    // Perform calculations
    const result = Num1 + Num2

    // Perform result
    alert(`${Num1} + ${Num2} = ${result}`)
}

function divide(){
    // Get 2 use input numbers
    // put parseFloat at the top is better, so you can change it.
    const Num1 = parseFloat(prompt("Enter your first number"))
    const Num2 = parseFloat(prompt("Enter your second number"))

    // Perform calculations
    const result = Num1 + Num2

    // Perform result
    alert(`${Num1} + ${Num2} = ${result}`)
}